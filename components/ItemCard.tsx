import Link from 'next/link';import Image from 'next/image';import { formatCredits } from '@/lib/utils';export default function ItemCard({item}:{item:any}){const img=item.images?.[0]?`/uploads/${item.images[0]}`:'/placeholder.svg';return(<Link href={`/items/${item.id}`} className='card flex flex-col gap-3'><div className='relative w-full h-48 rounded-xl overflow-hidden bg-gray-100'><Image src={img} alt={item.title} fill className='object-cover'/></div><div className='flex items-center justify-between'><h3 className='font-semibold'>{item.title}</h3><span className='badge'>{formatCredits(item.creditPrice)}</span></div><p className='text-sm text-gray-600 line-clamp-2'>{item.description}</p><div className='text-xs text-gray-500'>{item.location} • {item.category}</div></Link>)}